const HomePage = () => {
  return (
    <main>
      <section id="hero">...</section>

      <section id="layanan">
        {/* Layanan content */}
      </section>

      <section id="venue">
        {/* Venue content */}
      </section>

      <section id="dresses">
        {/* Dresses content */}
      </section>

      <section id="contact">
        {/* Contact content */}
      </section>
    </main>
  );
};